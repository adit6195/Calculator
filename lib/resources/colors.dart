import 'package:flutter/material.dart';

const operatorColor = Color(0xff4B5EFC);
const buttonColorDark = Color.fromARGB(255, 25, 25, 25);
const buttonColorLight = Colors.white;
const primaryColor = Color.fromARGB(255, 250, 250, 250);
const secondaryColor = Colors.black;